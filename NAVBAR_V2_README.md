# Navbar v2 — شاخ‌تر 🚀

## فایل‌های تغییر‌کرده

```
src/components/layout/Navbar.tsx   ← جایگزین کامل (تنها فایل تغییر‌کرده)
```

هیچ فایل دیگری لازم نیست عوض بشه. همه‌ی توکن‌های رنگ/سایه/z-index از
`tailwind.config.ts` و `src/styles/tokens.css` خودت استفاده شده و هیچ
پکیج جدیدی اضافه نشده (فقط آیکون‌های موجود `lucide-react`).

## چی عوض شد

| قبل | بعد |
|---|---|
| فقط یک ردیف سفید ساده | **دو طبقه**: utility strip تیره ۳۶px (تلفن، پشتیبانی ۲۴/۷، ارسال رایگان، پیگیری سفارش، گارانتی) + main bar |
| هدر ثابت و بی‌واکنش | **scroll-aware**: با اسکرول ارتفاع از ۷۲ به ۶۴ px جمع می‌شه، شیشه‌ای می‌شه (`bg-white/85 + backdrop-blur-xl`) و سایه می‌گیره |
| لینک‌های تخت با نقطه‌ی زیر | لینک‌ها داخل یک **pill container** با زیرخط گرادیانی primary→accent و micro-interaction |
| مگا منوی ۵۲۰px، فقط متن | مگا منوی **۶۶۰px** با آیکون گرادیانی هر دسته + **پنل تبلیغاتی مشاوره رایگان** + نوار اعتماد پایین (گارانتی / پشتیبانی) |
| باز شدن فقط با کلیک | باز شدن با **hover + delay ۱۴۰ms** (بدون پرش) و بسته شدن با Esc/کلیک بیرون + بک‌دراپ محو |
| بدون CTA | دکمه‌ی **«مشاوره رایگان»** گرادیانی با glow در سمت اکشن‌ها |
| بج سبد ساده | بج گرادیانی با `ring-2 ring-white` + انیمیشن سبد در hover |
| سرچ بدون راهنما | نشانگر `⌘K` داخل فیلد + `max-w-[420px]` |
| بدون فیدبک اسکرول | **نوار پیشرفت مطالعه** گرادیانی زیر هدر |
| شیت سرچ موبایل ساده | شیت با blur، گوشه‌های گرد و **چیپ‌های دسته‌بندی** برای دسترسی سریع |

## لوگو

دست‌نخورده باقی مونده — همون `Image` با `logo` از `useSiteSettings` و همون
fallback تایپوگرافی. فقط یک `scale-[1.03]` خیلی ملایم در hover و کوچک‌شدن
نرم ارتفاع هنگام اسکرول اضافه شده (چیزی از استایل تایپوگرافی عوض نشده).

## نصب

۱. فایل `src/components/layout/Navbar.tsx` رو با نسخه‌ی داخل زیپ جایگزین کن.
۲. `npm run dev` — تمام. (چیزی برای نصب یا کانفیگ نیست.)

## نکته‌ی کوچک

لینک‌های `/warranty` و `/tracking` در utility strip از `landingData.footer.services`
گرفته شدن؛ اگر این روت‌ها هنوز نیستن، فقط اون دو تا `<Link>` رو در بالای فایل حذف کن.
